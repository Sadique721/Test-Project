<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           targetNamespace="http://npm.redback.com"
           xmlns="http://npm.redback.com"
           elementFormDefault="qualified">



    <xs:element name="logonSubSession">
        <xs:complexType>
            <xs:sequence>
                <xs:element name="String_1" form="unqualified" type="xs:string"/>
                <xs:element name="String_2" form="unqualified" type="xs:string"/>
                <xs:element name="String_3" form="unqualified" type="xs:string"/>
            </xs:sequence>
        </xs:complexType>
    </xs:element>

    <!-- Response Element -->
    <xs:element name="logonSubSessionResponse">
        <xs:complexType>
            <xs:sequence>
                <xs:element name="result" type="xs:anyURI"/>
            </xs:sequence>
        </xs:complexType>
    </xs:element>

    <!-- MultiRef ResultType -->
    <xs:complexType name="ResultType">
        <xs:sequence>
            <xs:element name="href" type="xs:string"/>
        </xs:sequence>
    </xs:complexType>

    <!-- Fault Message for InvalidSubscriberAccountException -->
    <xs:complexType name="InvalidSubscriberAccountException">
        <xs:sequence>
            <xs:element name="responseMsg" type="xs:string"/>
            <xs:element name="responseCode" type="xs:int"/>
        </xs:sequence>
    </xs:complexType>


</xs:schema>
