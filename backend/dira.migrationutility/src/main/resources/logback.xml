<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Set the log file path -->
    <property name="LOG_PATH" value="C:/BSS_Migration/bss_migration/savbillt.bssmigrationutility/TestData/MigrationScriptLogs"/>

    <!-- Console Appender (prints logs to the console) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender (writes logs to a file with rolling policy) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Define log file location -->
        <file>${LOG_PATH}/application.log</file>

        <!-- Rolling policy to roll the log files daily -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/application-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory> <!-- Keep logs for 30 days -->
            <totalSizeCap>1GB</totalSizeCap> <!-- Total size cap for the log files -->
        </rollingPolicy>

        <!-- Log format pattern -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Set the root logger level and appenders -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
